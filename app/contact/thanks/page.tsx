export const metadata = { title: "Thanks — Simply Cucumber" };

export default function Thanks() {
  return (
    <section className="mx-auto max-w-2xl px-4 py-24 text-center">
      <h1 className="text-4xl font-[var(--font-playfair)] font-semibold">Message received.</h1>
      <p className="mt-4 text-gray-700">
        Thanks for reaching out — our team will get back to you within 1–2 business days.
      </p>
    </section>
  );
}
